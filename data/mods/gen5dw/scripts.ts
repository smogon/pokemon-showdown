/**
 * Dream World OU uses the BW1 battle engine and move availability.
 *
 * Its additional availability assumptions are expressed by the format's
 * `+Unreleased` rule and this mod's formats data.
 */
export const Scripts: ModdedBattleScriptsData = {
	inherit: 'gen5bw1',
	gen: 5,
};
