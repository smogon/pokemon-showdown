/**
 * Gold/Silver Spaceworld '97 — proof-of-concept mod.
 *
 * Mechanics are pure GSC (Gen 2); this mod only swaps in the beta base stats
 * and types that the 1997 Spaceworld demo shipped with. Data is sourced from
 * the pret/pokegold-spaceworld disassembly (data/pokemon/base_stats/*.asm).
 */
export const Scripts: ModdedBattleScriptsData = {
	inherit: 'gen2',
	gen: 2,
};
